const Connect = require("./connect");
const {
	Authentication, 
	AuthorizationCustomer, 
	AuthorizationSeller
} = require("./auth");

module.exports = {
	Connect,
	Authentication, 
	AuthorizationCustomer, 
	AuthorizationSeller
}
